Install openpyxl to work with excel files
    - sudo apt install python3-openpyxl

check the list of installed lib 
    - pip list

If you dont have exce reader install libre office( is free), download .deb and install
    - sudo dpkg -i *.deb

